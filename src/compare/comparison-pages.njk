---
pagination:
  data: comparisons
  size: 1
  alias: comparison
  addAllPagesToCollections: true
layout: comparison.njk
permalink: /compare/{{ comparison.id }}/
bodyClass: "v2-inter"
hideChrome: true
eleventyComputed:
  title: "{{ comparison.title | safe }} | Search Comparison | Zunkiree Labs"
  description: "{{ comparison.description | safe }}"
  comparisonId: "{{ comparison.id }}"
---

{# Individual comparison pages are generated from comparisons data #}
{# Content is rendered by the comparison.njk layout #}
