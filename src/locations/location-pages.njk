---
pagination:
  data: locations
  size: 1
  alias: location
layout: location.njk
permalink: /locations/{{ location.id }}/
eleventyComputed:
  title: "{{ location.title }}"
  description: "{{ location.description }}"
  locationId: "{{ location.id }}"
---

{# Individual location pages are generated from locations data #}
{# Content is rendered by the location.njk layout #}
